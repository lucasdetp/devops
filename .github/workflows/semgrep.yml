name: Semgrep CI

on:
  pull_request:
    branches:
      - main
      - 'feature/**'

jobs:
  semgrep:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install Semgrep
      run: |
        python -m pip install --upgrade pip
        pip install semgrep

    - name: Run Semgrep
      id: run_semgrep
      run: semgrep --config .semgrep.yml --error --metrics=off || echo "Semgrep failed" > semgrep-fail.txt

    - name: Archive Semgrep results
      if: failure()
      uses: actions/upload-artifact@v3
      with:
        name: semgrep-results
        path: semgrep.sarif

    - name: Display Semgrep results
      if: failure()
      run: cat semgrep.sarif

    - name: Comment on PR if XSS error
      if: failure()
      uses: actions/github-script@v5
      with:
        script: |
          const fs = require('fs');
          const resultsPath = 'semgrep.sarif';
          let message = '### Semgrep Analysis Found Issues\n\n';
          if (fs.existsSync(resultsPath)) {
            const results = JSON.parse(fs.readFileSync(resultsPath, 'utf8'));
            results.runs[0].results.forEach(result => {
              message += `**File:** ${result.locations[0].physicalLocation.artifactLocation.uri}\n**Line:** ${result.locations[0].physicalLocation.region.startLine}\n**Message:** ${result.message.text}\n\n`;
            });
          } else {
            message += 'No detailed results available.';
          }
          await github.rest.issues.createComment({
            issue_number: context.payload.pull_request.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: message
          });
